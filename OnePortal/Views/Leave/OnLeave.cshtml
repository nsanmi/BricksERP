@using HRM.DAL.Util;
@using PagedList;

@using PagedList.Mvc;
@model IPagedList<OnePortal.Models.ViewModels.LeaveR>

@{
    ViewBag.Title = "HR";


}
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>Leave Dashboard</h2>
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("Noticeboard","Admin")">Administration</a>
            </li>
            <li>
                <a>HRM</a>
            </li>
            <li class="active">
                <a>Leave</a>
            </li>
        </ol>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="wrapper wrapper-content animated fadeInUp">
            <div class="ibox">
                <div class="ibox-title">

                    <div>



                        <ul class="nav nav-tabs" id="pills-tab" role="tablist">
                            <li class="nav-item active">
                                <a class="nav-link" href="#all" id="pills-home-tab" data-toggle="pill" role="tab" aria-controls="pills-home" aria-selected="true">Presently on Leave</a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link" href="#Alluser" id="pills-alluser-tab" data-toggle="pill" role="tab" aria-controls="pills-home" aria-selected="false">All Leave User</a>
                            </li>
                            @*<li class="nav-item" id="resolved-tab" data-toggle="tab" role="tab">
                                <a class="nav-link" href="#resolved" id="pills-resolved-tab" data-toggle="pill" role="tab" aria-controls="pills-home" aria-selected="false">Presently on Leave</a>
                                    </li>*@


                            @*<li class="pull-right">
                                    <button class="btn btn-default bg-primary pull-right" onclick="post_process('rejected')" style="margin-left:5px"><i class="icon-pie5">REJECTED</i> </button>
                                    <button class="btn btn-default bg-primary pull-right" onclick="post_process('approved')" style="margin-left:5px"><i class="icon-pie5">APPROVED</i> </button>
                                    <button class="btn btn-default bg-danger pull-right" onclick="post_process('pending')" style="margin-left:5px"><i class="icon-pie5"></i>PENDING APPROVAL</button>
                                    <button class="btn btn-default bg-warning pull-right" onclick="post_process('scheduled')" style="margin-left:5px"><i class="icon-pie5"></i>SCHEDULED</button>
                                    <button class="btn btn-default bg-warning pull-right" onclick="post_process('taken')" style="margin-left:5px"><i class="icon-pie5"></i>TAKEN</button>
                                </li>*@


                        </ul>
                        <div class="tab-content" id="pills-tabContent">
                            <div class="tab-pane active" id="all" role="tabpanel" aria-labelledby="pills-all-tab">

                                <div id="" class="ibox-content">
                                    <div class="row m-b-sm m-t-sm">
                                        <div class="col-md-1">
                                            <button type="button" id="loading-example-btn" class="btn btn-white btn-sm"><i class="fa fa-refresh"></i> Refresh</button>
                                        </div>
                                        <form action="@Url.Action("HRIndex","Leave")" method="get">
                                            <div class="col-md-11">
                                                <div class="input-group">
                                                    <input type="text" placeholder="Search by lastname or firstname" name="search" value="@ViewBag.search" class="input-sm form-control">
                                                    <span class="input-group-btn">
                                                        <button type="submit" class="btn btn-sm btn-primary"> Go!</button>
                                                    </span>
                                                </div>
                                            </div>
                                        </form>
                                    </div>
                                    <div class="project-list">
                                        <div class="table-responsive">
                                            <table class="table table-bordered ">
                                                <thead>
                                                    <tr>
                                                        <th>Employee Name</th>
                                                        <th>Start Date</th>
                                                        <th>Leave end date</th>
                                                        <th>Days Taken</th>
                                                        <th>Days Pending approval</th>
                                                        <th>Days Scheduled</th>
                                                        <th>Days Canceled</th>
                                                        <th>Days Rejected</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    @foreach (var leave in Model)
                                                    {

                                                        if (leave.OnLeave == true)
                                                        {

                                                            <tr>
                                                                <td>@leave.EmployeeName</td>
                                                                <td>@leave.StartDate</td>
                                                                <td>@leave.EndDate</td>
                                                                <td>@leave.NumberOfDaysTaken</td>
                                                                <td>@leave.NumberOfDaysPendingApproval</td>
                                                                <td>@leave.NumberOfDaysScheduled</td>
                                                                <td>@leave.NumberOfDaysCanceled</td>
                                                                <td>@leave.NumberOfDaysRejected</td>
                                                            </tr>
                                                        }
                                                    }
                                                </tbody>
                                            </table>
                                        </div>
                                        <div id='Paging' style="text-align:center">
                                            Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)
                                            of @Model.PageCount
                                            @Html.PagedListPager(Model, page => Url.Action("HRIndex", new { page }))
                                        </div>
                                    </div>
                                </div>
                            </div>
                            @*<div class="tab-pane fade" id="resolved" role="tabpanel" aria-labelledby="pills-resolved-tab">
                                    <div id="" class="ibox-content">
                                        <div class="row m-b-sm m-t-sm">
                                            <div class="col-md-1">
                                                <button type="button" id="loading-example-btn" class="btn btn-white btn-sm"><i class="fa fa-refresh"></i> Refresh</button>
                                            </div>
                                            <form action="@Url.Action("HRIndex","Leave")" method="get">
                                                <div class="col-md-11">
                                                    <div class="input-group">
                                                        <input type="text" placeholder="Search by Type and Priority" name="search" value="@ViewBag.search" class="input-sm form-control">
                                                        <span class="input-group-btn">
                                                            <button type="submit" class="btn btn-sm btn-primary"> Go!</button>
                                                        </span>
                                                    </div>
                                                </div>
                                            </form>
                                        </div>
                                        <div class="project-list">
                                            <div class="table-responsive">
                                                <table class="table table-bordered ">
                                                    <thead>
                                                    <tr>
                                                        <th>Employee Name</th>
                                                        <th>Start Date</th>
                                                        <th>Leave end date</th>
                                                        <th>No. of Days</th>
                                                    </tr>
                                                    </thead>
                                                    <tbody>
                                                        @foreach (var leave in Model)
                                                        {


                                                            if (leave.OnLeave)
                                                            {
                                                                <tr>
                                                                    <td>@leave.EmployeeName</td>
                                                                    <td>@leave.StartDate</td>
                                                                    <td>@leave.EndDate</td>
                                                                    <td>@leave.NumberOfDays</td>
                                                                    <td>@leave.Status</td>
                                                                </tr>
                                                            }
                                                        }
                                                    </tbody>
                                                </table>
                                            </div>
                                            <div id='Paging' style="text-align:center">
                                                Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)
                                                of @Model.PageCount
                                                @Html.PagedListPager(Model, page => Url.Action("index", new { page }))
                                            </div>
                                        </div>
                                    </div>
                                </div>*@
                            <div class="tab-pane fade" id="Alluser" role="tabpanel" aria-labelledby="pills-alluser-tab">
                                <div id="" class="ibox-content">
                                    <div class="row m-b-sm m-t-sm">
                                        <div class="col-md-1">
                                            <button type="button" id="loading-example-btn" class="btn btn-white btn-sm"><i class="fa fa-refresh"></i> Refresh</button>
                                        </div>
                                        <form action="@Url.Action("HRIndex","Leave")" method="get">
                                            <div class="col-md-11">
                                                <div class="input-group">
                                                    <input type="text" placeholder="Search by Type and Priority" name="search" value="@ViewBag.search" class="input-sm form-control">
                                                    <span class="input-group-btn">
                                                        <button type="submit" class="btn btn-sm btn-primary"> Go!</button>
                                                    </span>
                                                </div>
                                            </div>
                                        </form>
                                    </div>
                                    <div class="project-list">
                                        <div class="table-responsive">
                                            <table class="table table-bordered ">
                                                <thead>
                                                    <tr>
                                                        <th>Employee Name</th>
                                                        <th>Start Date</th>
                                                        <th>Leave end date</th>
                                                        <th>Days Taken</th>
                                                        <th>Days Pending approval</th>
                                                        <th>Days Scheduled</th>
                                                        <th>Days Canceled</th>
                                                        <th>Days Rejected</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    @foreach (var leave in Model)
                                                    {

                                                        <tr>
                                                            <td>@leave.EmployeeName</td>
                                                            <td>@leave.StartDate</td>
                                                            <td>@leave.EndDate</td>
                                                            <td>@leave.NumberOfDaysTaken</td>
                                                            <td>@leave.NumberOfDaysPendingApproval</td>
                                                            <td>@leave.NumberOfDaysScheduled</td>
                                                            <td>@leave.NumberOfDaysCanceled</td>
                                                            <td>@leave.NumberOfDaysRejected</td>
                                                        </tr>

                                                    }
                                                </tbody>
                                            </table>
                                        </div>
                                        <div id='Paging' style="text-align:center">
                                            Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)
                                            of @Model.PageCount
                                            @Html.PagedListPager(Model, page => Url.Action("HRIndex", new { page }))
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



<style>
    .table td.text {
        max-width: 177px;
    }

        .table td.text span {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            display: inline-block;
            max-width: 100%;
        }

    .table td {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }


    th {
        font-weight: bold;
        font-size: 1em;
    }

    table {
        border: 1px;
        table-layout: fixed;
    }

    .table > tbody > tr > td, .table > thead > tr > th {
        padding: 6px 10px;
    }

    .table-striped > tbody > tr:nth-of-type(odd) {
    }

    .table-row {
        cursor: pointer;
    }
</style>




@section scripts{
    <script src="~/Content/libs/datapicker/bootstrap-datepicker.js"></script>
    <script src="~/Content/libs/dataTables/datatables.min.js"></script>
    <script src="~/Scripts/custom_fns/base.js"></script>
    <script src="~/Scripts/custom_fns/fn_leave.js"></script>
    <script>

        $(document).ready(function () {

            $('#data_1 .input-group.date').datepicker({
                format: 'yyyy-mm-dd',
                todayBtn: "linked",
                keyboardNavigation: false,
                forceParse: false,
                calendarWeeks: true,
                autoclose: true,
                daysOfWeekDisabled: [0, 6]
            });

        });

        function markComplainAsResolved(id) {
            alert(id);
        }

        function ResolvedComplain(id) {
            if (confirm("Are you sure that this complain is resolved?")) {
                $.ajax({
                    url: "@string.Format("{0}://{1}{2}", Request.Url.Scheme, Request.Url.Authority, Url.Content("~"))/Complain/Resolve/?Id=" + id,
                    context: document.body
                }).done(function (returnVal) {
                    alert(returnVal);
                    location.reload();
                });
            }
        }

        //$(document).on("click", "#modal", function () {
        //    var comment = $(this).data("id");


        //    $('#my_element_id').val(comment);
        //    alert(comment);
        //});


        $(function () {
            $(".identifyingClass").click(function () {
                var coment = $(this).data('id');
                $(".form-group #Comment_Container").val(coment);

            })
        });
    </script>
}

@section Styles{

    <link href="~/Content/libs/datapicker/datepicker3.css" rel="stylesheet">
    <link href="~/Content/libs/dataTables/datatables.min.css" rel="stylesheet">
}


